# Этап 1: Базовый образ
FROM python:3.11-slim

# Этап 2: Установка рабочей директории внутри контейнера
WORKDIR /app

# Этап 3: Установка зависимостей
# Копируем ТОЛЬКО файл с зависимостями из папки api
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Этап 4: Копирование кода приложения
# Копируем всё из папки api в рабочую папку контейнера
COPY . .

# Этап 5: Команда для запуска API
# Запускаем uvicorn, который будет слушать на порту 8000
# и будет доступен извне контейнера (host 0.0.0.0)
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
